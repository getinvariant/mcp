# === BACKEND (Vercel env vars) ===
# Provider API keys — sign up once, set in Vercel dashboard
ALPHA_VANTAGE_API_KEY=
OPENWEATHER_API_KEY=
EVERY_ORG_API_KEY=
OPENFDA_API_KEY=

# Comma-separated list of valid Procurement Labs keys
# Generate with: node -e "console.log('pl_' + require('crypto').randomBytes(16).toString('hex'))"
PL_VALID_KEYS=pl_demo_key_2026

# === MCP CLIENT (participant's env) ===
# PL_API_KEY=pl_demo_key_2026
# PL_BACKEND_URL=https://procurementlabs.vercel.app
