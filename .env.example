# Provider API keys — sign up once on each platform's free tier
# These are used by the MCP server internally. Users never see them.
ALPHA_VANTAGE_API_KEY=
OPENWEATHER_API_KEY=
EVERY_ORG_API_KEY=
OPENFDA_API_KEY=
