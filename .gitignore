# Node / build
node_modules/
dist/
*.tsbuildinfo
# Environment & secrets — never commit these
.env
.env.local
.env.*.local
.env.production
.env.development
secrets/
*.pem
*.key
*credentials*
*.csv

# Platform
.vercel
.railway
.DS_Store

# Editor
.idea/
.vscode/
*.swp

# Explicit (kept for paranoia even though *.csv covers it)
hackathon_keys.csv

# Demo files
demo/

# Local scratch / research notes (never commit — may contain keys)
bureau-implementation.txt
hyperspell-ui.txt
invariant-payment-research.txt
coder.md
schedueled_tasks.lock
CLAUDE.md